# CHANGELOG — ממשק מנהלת השיווק (/media)

## 11/06/2026 — MEDIA-INTERFACE-MOCKUP-FIDELITY (branch `feature/media-mockup-fidelity`)

הכשל שתוקן: מהמוקאפ של נירית נלקחו רק טוקנים (צבע/פונט) ונבנו מסכים אחרים.
הסבב הזה בונה את המסכים שציירה — אחד-לאחד.

### נוסף
- **סרגל-צד ימני** = הניווט הראשי: תיקייה לכל קורס אמיתי (דינמי מהדאטה),
  קידומי סושיאל, פריסות מדיה ותקציב, אישורים, + פתיחת קורס חדש, כרטיס-המנהלת.
- **מסך-בית מהמוקאפ**: ברכה אישית, 4 מונים אמיתיים, "מה מחכה לך עכשיו" עם
  פסי-צבע (דורש אישור/החלטה/חסר מידע), "פעילות אחרונה", "בקצרה — מצב
  הקמפיינים" עם % מהיעד העסקי (נרשמים מול יעד-מחזור) פר-קורס.
- **סביבת-עבודה לקורס** (מסך 1): טאבים טבלה/ציר-זמן/פעילים/אישורים; טבלה עם
  נושא/סוג/תצוגה/סטטוס-עבודה/אישור/נדרש-ממך/תאריך — שלוש עמודות-סטטוס נפרדות.
- **תיק-פריט** (מסך 2): פרטים, קבצים-וגרסאות (כולל Drive), סיכום בדיקות-מערכת
  אמיתי, ציר-פעילות מיומן-ההחלטות.
- **Review צמוד-גרסה** (מסך 3): תצוגת placement Feed/Story/Reel מנכסים אמיתיים,
  פאנל קופי (מה-payload האמיתי)/עיצוב/פרטים/הערות (חוזה sql/091), השוואה
  לגרסה קודמת, "אישור למרות ההערות" עם snapshot, בקשת-שינויים עם סיבה חובה.
- **גלריית אישורים** (מסך 4): כרטיסים עם thumbnail, V, קורס, "פתח Review";
  העשרת כרטיסי-המלצות ב-recommendation_text.
- **חלון בקשה חדשה** (מסך 5): טקסט חופשי, קובץ/קישור-Drive, עדיפות, מועד רצוי,
  שיוך-קורס אופציונלי; דרך דלת-הבקשות הקיימת. טופס-הבריף המלא מקושר מתוכו.
- דפי כל-הקורסים / פעילות / סושיאל / פריסות-ותקציב.

### מסנני-תצוגה קבועים (היגיינה — תצוגה בלבד, אפס מחיקת דאטה)
- תיקיות-טסט (E2E/Sim/diag/"(טרם זוהה)") לא מוצגות ולא נספרות; המונים
  תואמים לתוכן (103 ולא 200 ממתינים).
- חסמי-מערכת (make_routing/department_failed/critical_finding, owner≠מנהלת,
  שרידי-טסט) מנותבים מהפיד — 3 חסמים אמיתיים שלה במקום 66.
- "פעילות אחרונה" — רק החלטות-שלה + בוצע/טופל.

### נוסף בסבב המשך (הפרומפט המאוחד)
- **"+ פתיחת קורס חדש" פעיל** (`NewCourseModal.jsx`): טופס קצר — שם, קישור
  תיקיית-Drive, תאריך-עלייה, תקציב → יצירת תיקייה + בריף new_course בזרימה
  הקיימת; הקורס מופיע בסרגל מהדאטה מיד. (סריקת-Drive בקרון = שלב המשך.)
- **תגית-תיקייה** בכרטיסי "מה מחכה לך עכשיו" — כל פריט מציג את שיוכו בכל
  הופעה (קורס / פריסות מדיה ותקציב / פעילות בית-ספרית).

### הוסר
- `TopNav.jsx` — הוחלף בסרגל-הצד; אפס callers (קוד מת נמחק).
- קיצור "לממשק הקיים" מהדשבורד (לפי הנחיית-המשימה).

### בעיות ידועות
- נכסי Drive פרטיים לא נטענים ב-<img> אנונימי — fallback לקישור Drive;
  נדרש proxy ב-backend.
- מהלכי-ההיגיינה בדאטה עצמה ממתינים לאישור נירית (ספירות ב-PR).
